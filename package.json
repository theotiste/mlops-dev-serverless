{
  "name": "mlops-dev-serverless",
  "private": true,
  "devDependencies": {
    "serverless-python-requirements": "^6.0.0"
  }
},
  "scripts": {
    "deploy": "serverless deploy --stage dev --region eu-west-3",
    "remove": "serverless remove --stage dev --region eu-west-3",
    "print": "serverless print --stage dev --region eu-west-3",
    "package": "serverless package --stage dev --region eu-west-3"
  },
  "devDependencies": {
    "serverless": "^4.18.2",
    "serverless-prune-plugin": "^2.1.0",
    "serverless-python-requirements": "^6.1.2"
  }
}
